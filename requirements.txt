# ── Core ──────────────────────────────────────────────────────────────────────
django>=5.0,<6.0
python-decouple>=3.8          # .env management

# ── Data adapters ─────────────────────────────────────────────────────────────
mftool>=2.0
mstarpy>=10.0.0
yfinance>=0.2.55
yahooquery>=2.3.0             # fallback for fund holdings / sector data
requests>=2.31
nsepython>=0.1                # NSE index PE/PB/Div data via niftyindices.com (backtester v2)
nsefin>=0.1.5                 # NSE OHLC candlestick data
wbgapi>=1.0.14                # World Bank API — India CPI for inflation adjustment

# ── Analytics ─────────────────────────────────────────────────────────────────
pandas>=2.2
numpy>=1.26
scipy>=1.12
statsmodels>=0.14             # ARIMA forecasting (portfolio/services/forecasting.py)
scikit-learn>=1.4             # Ridge / RandomForest ML forecasting
rapidfuzz>=3.6                # fuzzy scheme name matching for CAS import

# ── Charts ────────────────────────────────────────────────────────────────────
plotly>=5.20

# ── Background tasks ──────────────────────────────────────────────────────────
django-q2>=1.7                # lightweight task queue; no Redis required in dev

# ── Portfolio ─────────────────────────────────────────────────────────────────
casparser>=0.6                # CAS PDF parsing

# ── Utilities ─────────────────────────────────────────────────────────────────
tabulate>=0.9
python-dateutil>=2.8

# ── Development ───────────────────────────────────────────────────────────────
jupyter
ipykernel

# ── Production ────────────────────────────────────────────────────────────────
gunicorn
psycopg2-binary               # PostgreSQL adapter
whitenoise>=6.6               # static file serving
weasyprint                    # PDF fund report export (apps/funds/report.py)
dj-database-url>=2.1          # DATABASE_URL parsing for prod (Render.com)
openpyxl>=3.1                 # Excel file reading (.xlsx) for portfolio upload
